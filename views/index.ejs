<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fruits Index</title>
  </head>
  <body>
    <h1>All Fruits</h1>
    <ul>
        <% fruits.forEach((fruit) => { %> <!-- Now using operator tags to loop through and display all of our fruits-->
            <li> <a href="/fruits/<%= fruit._id %>"> <%= fruit.name %> </a> </li> <!-- the fruit._id gets the ID from the object in the data-->
       <% }) %>
    </ul>

    <a href="/fruits/new">Add Fruits</a> <!-- link to go to form page -->

  </body>
</html>